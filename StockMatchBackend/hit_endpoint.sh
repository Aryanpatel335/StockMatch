#!/bin/bash

# Endpoint URL
URL="https://stockmatch-spring-api.onrender.com"

# Send a GET request to the URL
curl -s $URL

# You can redirect the output to a log file or handle it in any other way if needed
